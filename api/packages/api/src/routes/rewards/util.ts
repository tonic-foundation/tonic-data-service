export interface Reward {
  // max_price_multiplier: number;
  // eligible_bp_distance: number;
  // time_divisor: number;
  account_id: string;
  reward: number;
  reward_date: Date;
  paid: boolean;
}
